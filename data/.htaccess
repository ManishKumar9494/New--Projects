# Block direct web access to the JSON data files (blogs.json, leads.json).
# The site reads/writes them only through api.php.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
